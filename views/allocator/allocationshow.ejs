<% include ../partials/header %>
<% include ../partials/header2 %>
<div class="container">
	<p><%= allocatorinfo.pickTarget +' ' %> to Pick. <%= allocatorinfo.tipTarget +' ' %> to Tip.</p>
	<h1 class="myh"><%= allocatorinfo.time.toDateString() %>(Currently no middles or nights)</h1>
</div>
<div class="container container1">
	<div class="pick">
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Pick Manager:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/pickmanagershow"><span class="noprint">Pick Manager:</span></a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundPickmanager.forEach(function(foundPickmanager){ %>
		<% if (foundPickmanager.author.id.equals(allocatorinfo.id) && foundPickmanager.colleagueId === foundColleague.id) { %>
		<p class="capitalized"><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Pick:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/pickershow">Pick:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundPicker.forEach(function(foundPicker){ %>
		<% if (foundPicker.author.id.equals(allocatorinfo.id) && foundPicker.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<%# office ide %>
		<h3>
		<% if (foundOffice) { %>
		<% if (allocatorinfo.isAllocated == true) { %>
		Office:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/officeshow">Office:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundOffice.forEach(function(foundOffice){ %>
		<% if (foundOffice.author.id.equals(allocatorinfo.id) && foundOffice.colleagueId === foundColleague.id) { %>
		<p class="capitalized"><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<% } %>
		<h3>
		<% if (foundManualpick) { %>
		<% if (allocatorinfo.isAllocated == true) { %>
		Checking:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/manualpickshow">Checking:</a>
		<% } %>
		</h3>
		<% foundManualpick.forEach(function(foundManualpick){ %>
		<% if (foundManualpick.author.id.equals(allocatorinfo.id)) { %>
		<p><a href="/colleagues/<%= foundManualpick.colleagueId %>" title=""><%= foundManualpick.text %></a></p>
		<% } %>
		<% }); %>
		<% } %>
	</div>
	<div class="chutes">
		<h3>
		<% if (allocatorinfo.isAllocated == true) { -%>
		Chill Manager:
		<% } else { -%>
		<a href="/allocator/<%= allocatorinfo.id %>/chutemanagershow"><span class="noprint">Chill Manager:</span></a>
		<% } -%>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundChutemanager.forEach(function(foundChutemanager){ %>
		<% if (foundChutemanager.author.id.equals(allocatorinfo.id) && foundChutemanager.colleagueId === foundColleague.id) { %>
		<p class="capitalized"><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Chill Pick:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/chuteshow">Chill Pick:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundChute.forEach(function(foundChute){ %>
		<% if (foundChute.author.id.equals(allocatorinfo.id) && foundChute.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
	</div>
	<div class="trucks">
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		G. In Manager:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/truckmanagershow"><span class="noprint">G. In Manager:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundTruckmanager.forEach(function(foundTruckmanager){ %>
		<% if (foundTruckmanager.author.id.equals(allocatorinfo.id) && foundTruckmanager.colleagueId === foundColleague.id) { %>
		<p class="capitalized"><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Reach Truck:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/drivershow">Reach Truck:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundDriver.forEach(function(foundDriver){ %>
		<% if (foundDriver.author.id.equals(allocatorinfo.id) && foundDriver.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Scissor lift:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/rtshow">Scissor lift:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundRt.forEach(function(foundRt){ %>
		<% if (foundRt.author.id.equals(allocatorinfo.id) && foundRt.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Run:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/runnershow">Run:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundRunner.forEach(function(foundRunner){ %>
		<% if (foundRunner.author.id.equals(allocatorinfo.id) && foundRunner.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Baler:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/balershow">Baler:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundBaler.forEach(function(foundBaler){ %>
		<% if (foundBaler.author.id.equals(allocatorinfo.id) && foundBaler.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Union:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/unionshow">Union:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundUnion.forEach(function(foundUnion){ %>
		<% if (foundUnion.author.id.equals(allocatorinfo.id) && foundUnion.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
	</div>
	<div class="tip">
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Tip:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/tippershow">Tip:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundTipper.forEach(function(foundTipper){ %>
		<% if (foundTipper.author.id.equals(allocatorinfo.id) && foundTipper.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Yard:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/yardshow">Yard:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundYard.forEach(function(foundYard){ %>
		<% if (foundYard.author.id.equals(allocatorinfo.id) && foundYard.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Training:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/trainingshow">Training:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundTraining.forEach(function(foundTraining){ %>
		<% if (foundTraining.author.id.equals(allocatorinfo.id) && foundTraining.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
	</div>
	<div class="loading">
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Loading:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/loadershow">Loading:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundLoader.forEach(function(foundLoader){ %>
		<% if (foundLoader.author.id.equals(allocatorinfo.id) && foundLoader.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Racking:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/rackingshow">Racking:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundRacking.forEach(function(foundRacking){ %>
		<% if (foundRacking.author.id.equals(allocatorinfo.id) && foundRacking.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
	</div>
	<div class="mts">
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		De-Kit:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/mtsshow">De-kit:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundMts.forEach(function(foundMts){ %>
		<% if (foundMts.author.id.equals(allocatorinfo.id) && foundMts.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<!-- Recoup commented out -->
		<!-- <h3> -->
		<%# if (allocatorinfo.isAllocated == true) { %>
		<!-- Recoup: -->
		<%# } else { %>
		<!-- <a href="/allocator/<%= allocatorinfo.id %>/rbshow">Recoup:</a> -->
		<%# } %>
		<!-- </h3> -->
		<%# modosit %>
		<%#  foundColleague.forEach(function(foundColleague){ %>
		<%# foundRb.forEach(function(foundRb){ %>
		<%# if (foundRb.author.id.equals(allocatorinfo.id) && foundRb.colleagueId === foundColleague.id) { %>
		<!-- <p><a href="/colleagues/<%# foundColleague.id %>" title=""><%# foundColleague.fullName %></a></p> -->
		<%# } %>
		<%# }); %>
		<%# }); %>
		<%# modosit %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Inbound clerk:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/cscshow">Inbound clerk:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundCsc.forEach(function(foundCsc){ %>
		<% if (foundCsc.author.id.equals(allocatorinfo.id) && foundCsc.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Outbound clerk:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/vlsshow">Outbound clerk:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundVls.forEach(function(foundVls){ %>
		<% if (foundVls.author.id.equals(allocatorinfo.id) && foundVls.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
		<h3>
		<% if (allocatorinfo.isAllocated == true) { %>
		Hygiene:
		<% } else { %>
		<a href="/allocator/<%= allocatorinfo.id %>/hygieneshow">Hygiene:</a>
		<% } %>
		</h3>
		<%  foundColleague.forEach(function(foundColleague){ %>
		<% foundHygiene.forEach(function(foundHygiene){ %>
		<% if (foundHygiene.author.id.equals(allocatorinfo.id) && foundHygiene.colleagueId === foundColleague.id) { %>
		<p><a href="/colleagues/<%= foundColleague.id %>" title=""><%= foundColleague.fullName %></a></p>
		<% } %>
		<% }); %>
		<% }); %>
	</div>
</div>
<br>
<div class="container">
	<div class="col-sm6 col-md-4 col-md-offset-4 col-sm-offset-3">
		<%if (currentUser && allocatorinfo.isAllocated == true && currentUser.isAdmin) { %>
		<div class="form-group">
			<form action="/allocator/<%= allocatorinfo._id %>?_method=DELETE" method="POST">
				<button class="btn btn-primary btn-block goldblack">Delete</button>
			</form>
		</div>
		<%}else if (currentUser && currentUser.isAdmin){%>
		<div class="form-group">
			<form action="/allocator/<%= allocatorinfo._id %>" method="POST">
				<input type="submit" class="btn btn-primary btn-block blackgold" value="Automatic job allocation">
			</form>
		</div>
		<hr class="style-four">
		<div class="form-group">
			<form action="/allocator/<%= allocatorinfo._id %>?_method=DELETE" method="POST">
				<button class="btn btn-primary btn-block goldblack">Delete</button>
			</form>
		</div>
		<%}%>
	</div>
</div>
<% include ../partials/footer %>